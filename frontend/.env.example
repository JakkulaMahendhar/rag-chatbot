# Base URL of the FastAPI backend (see ../render.yaml / ../docker-compose.yml
# for the actual backend service). Only NEXT_PUBLIC_-prefixed variables are
# ever sent to the browser - never put secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8000
